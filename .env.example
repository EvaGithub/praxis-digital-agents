# Option A — Google AI Studio (simplest for demo)
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=your_ai_studio_key_here

# Option B — Vertex AI (what you used for Agent Runtime deployment)
# GOOGLE_GENAI_USE_VERTEXAI=TRUE
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_CLOUD_LOCATION=europe-west6

# Pipeline settings (optional — sensible defaults are used if omitted)
# PURSUE_THRESHOLD=4   # min. failed checks (out of 12) before building a full package
# MAX_LEADS=5          # max. practices audited per run
